from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='using various multiple linear regression models for predicting home prices',
    author='Jesse',
    license='MIT',
)
